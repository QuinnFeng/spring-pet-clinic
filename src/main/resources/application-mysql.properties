# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/sys
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${MYSQL_USER:zfeng}
spring.datasource.password=${MYSQL_PASS:mercury}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
